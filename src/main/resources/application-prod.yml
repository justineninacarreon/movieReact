spring:
  profiles:
    active: prod

  datasource:
    url: jdbc:mysql://localhost:3306/movie_db?useSSL=false
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true